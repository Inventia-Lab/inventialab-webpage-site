{
  "name": "inventialab-webpage-site",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "cra-template": "1.3.0",
    "framer-motion": "^12.29.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-helmet": "^6.1.0",
    "react-intersection-observer": "^10.0.2",
    "react-router-dom": "^7.12.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "stage-deploy": "react-scripts build && firebase hosting:channel:deploy --expires 5d inventia-previous",
    "deploy": "react-scripts build && firebase deploy"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
